# Core
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
Pillow>=10.0.0

# Car part localization (Roboflow Universe YOLOv8 model)
inference-sdk>=0.9.0

# Car damage type classification (Hugging Face, runs locally, no API key)
transformers>=4.40.0
torch>=2.0.0
safetensors>=0.4.0

# Object type classification (car/laptop/package) + laptop/package damage
# + transcript matching -- all via Gemini Flash free tier
google-genai>=0.3.0

# Blur / image quality check
opencv-python-headless>=4.8.0easyocr>=1.7.0
